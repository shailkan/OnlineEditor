CREATE TABLE IF NOT EXISTS employee(
                                       empId INT PRIMARY KEY,
                                       role INT,
                                       username TEXT,
                                       password TEXT
);